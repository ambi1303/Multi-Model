import React from 'react';
import { Box, Typography, Paper } from '@mui/material';

const AdminPage: React.FC = () => {
  return (
    <div>
      <h1>Admin Page</h1>
      <p>This is the admin dashboard. Only authorized users can see this.</p>
    </div>
  );
};

export default AdminPage; 